import React from 'react';
import Headers from "./Headers"

const Login = (props) => {
    return(
        <div>
            <Headers name="Login 페이지" content="로그인 페이지입니다." />
        </div>
    )
}

export default Login;